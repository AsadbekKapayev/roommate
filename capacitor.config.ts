import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kz.roommate.mobile',
  appName: 'Roommate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
